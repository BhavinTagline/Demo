<h1>Organization Detail</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <table class="table table-hover">
      <tbody class="thead-light">
        <tr>
          <td><%= label :organization, "Organization", class: "tbl_label" %></td>
          <td><%= label :organization, @organization.name, class: "tbl_label" %></td>
        </tr>
        <tr>
          <td><%= label :department, "Department", class: "tbl_label" %></td>
          <td><%= label :department, @departments, class: "tbl_label" %></td>
        </tr>
      </tbody>
    </table>
    <p align="right"><%= link_to "Back", new_employee_user_path, class: "btn btn-primary" %></p>
  </div>
</div>
